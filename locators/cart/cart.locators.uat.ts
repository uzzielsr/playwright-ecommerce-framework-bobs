/**
 * Cart page locators.
 * These may change depending on the current ENV (e.g., qa, uat, prod).
 */
export const cartLocators = {
    proceedToCheckoutButton: 'button.bobs-button--cart-action.bobs-button--default:has(span:text-is("Check out"))',
    closePopupButton: '#closeIconContainer',
};